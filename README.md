# Vue2 Admin System

一个基于 Vue2 和 Element UI 的后台管理系统模板

## ✨ 特性

- 🎨 **现代化布局**: 使用 Element UI Container 实现的响应式布局
- 📊 **用户管理**: 完整的 CRUD 功能 (增删改查)
- 🔍 **实时搜索**: 支持用户名、邮箱、角色的模糊搜索
- 🎭 **弹窗编辑**: 使用 子组件弹窗 实现新增和编辑功能
- ✅ **表单校验**: 完整的表单验证（含自定义校验器）
- 🗑️ **批量操作**: 支持多选和批量删除
- 🎯 **SASS 变量**: 统一的颜色和样式管理
- 📱 **响应式设计**: 适配移动端和桌面端

## 📦 技术栈

- Vue 2.6
- Element UI 2.4.5
- Vue CLI 5.0
- SASS/SCSS

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run serve
```

### 生产构建

```bash
npm run build
```

### 代码检查

```bash
npm run lint
```

## 📁 项目结构

```
vue2-admin-system/
├── public/              # 静态资源
├── src/
│   ├── assets/          # 资源文件
│   │   └── styles/      # 样式文件
│   │       ├── _variables.scss  # SASS 变量
│   │       └── main.scss        # 全局样式
│   ├── components/      # 组件
│   │   ├── SiderBar.vue       # 侧边栏
│   │   ├── TopHeader.vue      # 顶栏
│   │   ├── UserFormModal.vue  # 用户表单弹窗
│   │   └── UserList.vue       # 用户列表（待开发）
│   ├── plugins/         # 插件配置
│   │   └── element.js         # Element UI 按需引入配置
│   ├── views/           # 页面视图
│   │   └── UserManagement.vue # 用户管理页面
│   ├── App.vue          # 根组件
│   └── main.js          # 入口文件
└── vue.config.js        # Vue CLI 配置
```

## 🎯 功能模块

### 用户管理

- ✅ 用户列表展示（Element UI Table）
- ✅ 新增用户（Element UI Form + Dialog）
- ✅ 编辑用户
- ✅ 删除用户（MessageBox 确认）
- ✅ 多选/部分全选
- ✅ 批量删除（MessageBox 确认）
- ✅ 实时搜索过滤
- ✅ 表单校验（含自定义校验器）
- ✅ 响应式表格（固定列、溢出提示）

## 📝 布局说明

项目采用 **Element UI Container** 布局组件实现后台管理系统经典布局:

```
┌─────────────────────────────────────────┐
│  el-aside  │    el-header               │
│            ├────────────────────────────┤
│            │    el-main                 │
│            │                            │
└─────────────────────────────────────────┘
```

## 🎨 样式系统

使用 SASS 变量统一管理颜色和尺寸，结合 Element UI 主题定制:

```scss
$primary-color: #409eff; // Element UI 主色
$danger-color: #f56c6c;
$base-padding: 20px;
$top-header-height: 60px;
```

### 颜色系统

- **主色调**: 用于主要交互元素和强调内容
- **辅助色**: 用于次要按钮和信息提示
- **背景色**: 提供良好的内容区分
- **文本色**: 确保良好的可读性

## 🔧 Element UI 配置

### 按需引入

项目采用 **babel-plugin-component** 实现按需引入，减小打包体积:

```js
// babel.config.js
plugins: [
  [
    "component",
    {
      libraryName: "element-ui",
      styleLibraryName: "theme-chalk",
    },
  ],
];
```

### 已引入组件

- **布局**: Container, Aside, Header, Main, Footer
- **表单**: Form, FormItem, Input, Select, Option
- **数据**: Table, TableColumn
- **反馈**: Message, MessageBox
- **基础**: Button

### 添加新组件

在 `src/plugins/element.js` 中引入所需组件:

```js
import { YourComponent } from "element-ui";
Vue.use(YourComponent);
```

## 🌐 GitHub Pages 自动部署

项目已配置 GitHub Actions 自动部署。

### 部署步骤

1. **配置 vue.config.js**

   ```javascript
   publicPath: process.env.NODE_ENV === "production"
     ? "/vue2-admin-system/"
     : "/";
   ```

2. **启用 GitHub Pages**

   - 进入仓库 Settings → Pages
   - Source 选择 "GitHub Actions"

3. **推送代码**

   ```bash
   git add .
   git commit -m "Add GitHub Actions deploy"
   git push origin master
   ```

4. **访问网站**
   - 部署完成后访问: `https://你的用户名.github.io/vue2-admin-system/`

## 🚧 下一步开发

- [ ] 使用 dialog 组件替换自定义弹窗
- [ ] 分页功能实现
- [ ] 路由系统（Vue Router）
- [ ] 状态管理（Vuex）
- [ ] 权限管理模块
- [ ] 数据可视化（图表）
- [ ] 更多 CRUD 模块（商品、订单等）
- [ ] 主题切换功能
- [ ] 国际化（i18n）

## 📚 学习收获

- Element UI 组件按需引入与配置
- 复选框与数组绑定实现批量操作
- 表单自定义校验器的使用
- MessageBox 确认提示的最佳实践
- Vue 组件间通信（props / emit）
- 响应式表格设计（min-width / show-overflow-tooltip）
