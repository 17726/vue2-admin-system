# Vue2 Admin System

一个基于 Vue2 的后台管理系统模板

## ✨ 特性

- 🎨 **现代化布局**: 使用 CSS Grid 实现的响应式布局
- 📊 **用户管理**: 完整的 CRUD 功能 (增删改查)
- 🔍 **实时搜索**: 支持用户名、邮箱、角色的模糊搜索
- 🎭 **弹窗编辑**: 使用 Modal 实现新增和编辑功能
- 🎯 **SASS 变量**: 统一的颜色和样式管理

## 📦 技术栈

- Vue 2.6
- Vue CLI 5.0
- SASS/SCSS
- CSS Grid Layout

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run serve
```

### 生产构建

```bash
npm run build
```

### 代码检查

```bash
npm run lint
```

## 📁 项目结构

```
vue2-admin-system/
├── public/              # 静态资源
├── src/
│   ├── assets/          # 资源文件
│   │   └── styles/      # 样式文件
│   │       ├── _variables.scss  # SASS 变量
│   │       └── main.scss        # 全局样式
│   ├── components/      # 组件
│   │   ├── SiderBar.vue       # 侧边栏
│   │   ├── TopHeader.vue      # 顶栏
│   │   └── UserFormModal.vue  # 用户表单弹窗
│   ├── views/           # 页面视图
│   │   └── UserManagement.vue # 用户管理页面
│   ├── App.vue          # 根组件
│   └── main.js          # 入口文件
└── vue.config.js        # Vue CLI 配置
```

## 🎯 功能模块

### 用户管理

- ✅ 用户列表展示
- ✅ 新增用户
- ✅ 编辑用户
- ✅ 删除用户
- ✅ 批量删除
- ✅ 实时搜索过滤
- ✅ 角色标签展示

## 📝 布局说明

项目采用 **CSS Grid Layout** 实现后台管理系统经典布局:

```
┌─────────────────────────────────────────┐
│  SiderBar  │    TopHeader               │
│            ├────────────────────────────┤
│            │    Main Content            │
│            │                            │
└─────────────────────────────────────────┘
```

## 🎨 样式系统

使用 SASS 变量统一管理颜色和尺寸:

```scss
$primary-color: hsl(204, 100%, 50%);
$danger-color: hsl(348, 100%, 61%);
$base-padding: 20px;
$top-header-height: 60px;
```

### 颜色系统

- **主色调**: 用于主要交互元素和强调内容
- **辅助色**: 用于次要按钮和信息提示
- **背景色**: 提供良好的内容区分
- **文本色**: 确保良好的可读性

## 🌐 GitHub Pages 自动部署

项目已配置 GitHub Actions 自动部署。

### 部署步骤

1. **配置 vue.config.js**

   ```javascript
   publicPath: process.env.NODE_ENV === "production"
     ? "/vue2-admin-system/"
     : "/";
   ```

2. **启用 GitHub Pages**

   - 进入仓库 Settings → Pages
   - Source 选择 "GitHub Actions"

3. **推送代码**

   ```bash
   git add .
   git commit -m "Add GitHub Actions deploy"
   git push origin master
   ```

4. **访问网站**
   - 部署完成后访问: `https://你的用户名.github.io/vue2-admin-system/`

## 下一步开发

- 优化响应式设计，提升移动端体验
- 样式 ai 优化
  - 优化 UserManagement 样式效果
  - 优化表格样式：table
  - 优化按钮样式：button
  - 优化弹窗样式：modal
- 集成更多功能模块，如权限管理、数据可视化等
- Router
- 状态管理（Vuex）
